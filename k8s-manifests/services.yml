apiVersion: v1
kind: Service
metadata:
  name: angularspree-service
  namespace: angularspree-ns
spec:
  selector:
    app: angularspree
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4200